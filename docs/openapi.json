{
  "openapi" : "3.0.1",
  "info" : {
    "license" : {
      "name" : "Apache-2.0",
      "url" : "https://www.apache.org/licenses/LICENSE-2.0"
    },
    "title" : "ATP-TDM"
  },
  "security" : [
    {
      "Bearer Authentication" : [ ]
    }
  ],
  "tags" : [
    {
      "description" : "Version of the service.",
      "name" : "versions-controller"
    },
    {
      "description" : "Operations that ATP actions call on a test data table. A request names the table by projectName, envName, systemName, and title-table; projectName is a project name from PROJECTS_INFO. When envName or systemName is missing, the first table with that title in the project is used. A trailing timestamp such as \" 2024-05-01T10:15:30\" in envName is ignored. Most operations return one ResponseMessage per row request, with type SUCCESS or ERROR and HTTP status 200 in both cases, so one failed row request does not stop the others.",
      "name" : "atp-action-controller"
    },
    {
      "description" : "Versions of test data operations that take their parameters in the request body.",
      "name" : "test-data-controller-v2"
    },
    {
      "description" : "Dynamic environments: environments, systems, and connections created at run time and stored in the database. The project must be listed in PROJECTS_INFO. The request body can hold the fields at the top level or inside an environment object.",
      "name" : "atp-env-controller"
    },
    {
      "description" : "Refresh settings of test data tables. A refresh deletes the rows of a table and fills it again from the SQL query the table was imported with.",
      "name" : "data-refresh-controller"
    },
    {
      "description" : "Test data tables: import, read, occupy and release rows, export, and maintenance. A test data table has a title shown in the UI and a generated database table name; the tableName parameters take the database table name.",
      "name" : "test-data-controller"
    },
    {
      "description" : "Cleanup settings of test data tables: which rows to delete, found by an SQL query, a date, or a cleaner class, and the cron schedule to do it on.",
      "name" : "data-cleanup-controller"
    },
    {
      "description" : "Statistics on test data tables and the email reports sent on a schedule. Statistics without systemId group the tables of a project by table title, with the per-system values in details.",
      "name" : "statistics-controller"
    },
    {
      "description" : "Projects, environments, and systems that test data tables belong to. Projects come from PROJECTS_INFO; environments and systems are the dynamic environments stored in the database.",
      "name" : "environments-controller"
    }
  ],
  "paths" : {
    "/api/tdm/alter/created/when" : {
      "get" : {
        "description" : "Adds the CREATED_WHEN column to every test data table that lacks it. A migration for tables created before the column existed.",
        "operationId" : "alterCreatedWhenColumn",
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        },
        "summary" : "Add the CREATED_WHEN column to all tables",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/alter/occupy/statistic" : {
      "get" : {
        "description" : "Adds an occupation record to the statistics for every occupied row of every table. A migration for rows occupied before the statistics existed.",
        "operationId" : "alterOccupyStatistic",
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        },
        "summary" : "Fill in the occupation statistics from occupied rows",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/change/title" : {
      "put" : {
        "description" : "Changes the title of the table and of its occupation statistics. Returns whether the title changed.",
        "operationId" : "changeTestDataTitle",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ChangeTitleRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "boolean"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Rename a table",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/cleanup/config" : {
      "post" : {
        "description" : "Saves the cleanup configuration for the table in tableName and for the tables of the same system in the environments listed in environmentsList, and schedules it. queryTimeout must be between 1 and EXTERNAL_QUERY_MAX_TIMEOUT seconds (3600 by default); a value out of range returns HTTP 400.",
        "operationId" : "saveCleanupConfig",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/CleanupSettings"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/CleanupSettings"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Save cleanup settings",
        "tags" : [
          "data-cleanup-controller"
        ]
      }
    },
    "/api/tdm/cleanup/config/{id}" : {
      "get" : {
        "description" : "Returns the cleanup configuration and the environments of the tables that use it. tableName is not set in the response.",
        "operationId" : "getCleanupConfig",
        "parameters" : [
          {
            "description" : "Cleanup configuration ID.",
            "in" : "path",
            "name" : "id",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/CleanupSettings"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Get cleanup settings",
        "tags" : [
          "data-cleanup-controller"
        ]
      }
    },
    "/api/tdm/cleanup/fill/cleanup/type" : {
      "get" : {
        "description" : "Sets the type of every cleanup configuration from the field it uses: DATE when searchDate is set, otherwise SQL when searchSql is set, otherwise CLASS when searchClass is set. A migration for configurations saved before the type existed.",
        "operationId" : "fillCleanupTypeColumn",
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        },
        "summary" : "Fill in the cleanup type of existing configurations",
        "tags" : [
          "data-cleanup-controller"
        ]
      }
    },
    "/api/tdm/cleanup/next/run" : {
      "get" : {
        "description" : "Returns the next time the Quartz cron expression fires after now, as a JSON string in \"EEE MMM dd HH:mm:ss zzz yyyy\" format.",
        "operationId" : "getNextCleanupRun",
        "parameters" : [
          {
            "description" : "Quartz cron expression, starting with the seconds field.",
            "in" : "query",
            "name" : "cronExpression",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "string"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Get the next run time of a schedule",
        "tags" : [
          "data-cleanup-controller"
        ]
      }
    },
    "/api/tdm/cleanup/run" : {
      "post" : {
        "description" : "Runs the cleanup configuration from the request body, without saving it, on the table in tableName and on the tables of the same system in the environments listed in environmentsList. Returns the number of checked and removed rows per table.",
        "operationId" : "runDataCleanup",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/CleanupSettings"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/CleanupResults"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Run a cleanup now",
        "tags" : [
          "data-cleanup-controller"
        ]
      }
    },
    "/api/tdm/data/available/recalculate" : {
      "get" : {
        "description" : "Returns, for each table of the system in the environment that has the column, the distinct values of the column.",
        "operationId" : "getDistinctColumnValues",
        "parameters" : [
          {
            "description" : "System ID.",
            "in" : "query",
            "name" : "systemId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "Name of the column.",
            "in" : "query",
            "name" : "columnName",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          },
          {
            "description" : "Environment ID.",
            "in" : "query",
            "name" : "environmentId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/TableColumnValues"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "List column values across the tables of a system",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/data/refresh/config" : {
      "post" : {
        "description" : "Saves the refresh configuration and the query timeout for the table, and schedules the refresh. With allEnv set in the configuration, it also applies to the tables of the project with the same title and the same import query. A queryTimeout out of range, or a table whose environment cannot run SQL queries, returns HTTP 400.",
        "operationId" : "saveRefreshConfig",
        "parameters" : [
          {
            "description" : "Database table name of the test data table.",
            "in" : "query",
            "name" : "tableName",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          },
          {
            "description" : "Timeout of the import query, in seconds, from 1 to EXTERNAL_QUERY_MAX_TIMEOUT (3600 by default).",
            "in" : "query",
            "name" : "queryTimeout",
            "required" : true,
            "schema" : {
              "type" : "integer",
              "format" : "int32"
            }
          }
        ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/TestDataRefreshConfig"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/TestDataRefreshConfig"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Save refresh settings",
        "tags" : [
          "data-refresh-controller"
        ]
      }
    },
    "/api/tdm/data/refresh/config/{id}" : {
      "get" : {
        "operationId" : "getRefreshConfig",
        "parameters" : [
          {
            "description" : "Refresh configuration ID.",
            "in" : "path",
            "name" : "id",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/TestDataRefreshConfig"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Get refresh settings",
        "tags" : [
          "data-refresh-controller"
        ]
      }
    },
    "/api/tdm/data/refresh/next/run" : {
      "get" : {
        "description" : "Returns the next time the Quartz cron expression fires after now, as a JSON string in \"EEE MMM dd HH:mm:ss zzz yyyy\" format.",
        "operationId" : "getNextRefreshRun",
        "parameters" : [
          {
            "description" : "Quartz cron expression, starting with the seconds field.",
            "in" : "query",
            "name" : "cronExpression",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "string"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Get the next run time of a schedule",
        "tags" : [
          "data-refresh-controller"
        ]
      }
    },
    "/api/tdm/data/refresh/run" : {
      "post" : {
        "description" : "Deletes all rows of the table, occupied rows included, and runs the import query again with the saved query timeout. Returns the number of loaded rows per table.",
        "operationId" : "runDataRefresh",
        "parameters" : [
          {
            "description" : "Database table name of the test data table.",
            "in" : "query",
            "name" : "tableName",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          },
          {
            "description" : "Not used; the refresh runs with the query timeout saved for the table.",
            "in" : "query",
            "name" : "queryTimeout",
            "required" : true,
            "schema" : {
              "type" : "integer",
              "format" : "int32"
            }
          },
          {
            "description" : "Also refreshes the tables of the project with the same title and the same import query.",
            "in" : "query",
            "name" : "allEnv",
            "required" : true,
            "schema" : {
              "type" : "boolean"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/RefreshResults"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Run a refresh now",
        "tags" : [
          "data-refresh-controller"
        ]
      }
    },
    "/api/tdm/delete/rows" : {
      "put" : {
        "operationId" : "deleteTestDataTableRows",
        "parameters" : [
          {
            "description" : "Database table name of the test data table.",
            "in" : "query",
            "name" : "tableName",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          }
        ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "type" : "array",
                "description" : "ROW_ID values of the rows.",
                "items" : {
                  "type" : "string",
                  "format" : "uuid"
                }
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        },
        "summary" : "Delete rows by ID",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/download/csv" : {
      "get" : {
        "operationId" : "getTestDataTableAsCsvFile",
        "parameters" : [
          {
            "description" : "Database table name of the test data table.",
            "in" : "query",
            "name" : "tableName",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "string",
                  "format" : "binary"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Download a table as a CSV file",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/download/excel" : {
      "get" : {
        "operationId" : "getTestDataTableAsExcelFile",
        "parameters" : [
          {
            "description" : "Database table name of the test data table.",
            "in" : "query",
            "name" : "tableName",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "string",
                  "format" : "binary"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Download a table as an Excel file",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/environment/tables/list" : {
      "get" : {
        "description" : "Returns a map of database table names to table titles.",
        "operationId" : "getTestDataTablesListByEnvironment",
        "parameters" : [
          {
            "description" : "Project ID.",
            "in" : "query",
            "name" : "projectId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "Environment ID.",
            "in" : "query",
            "name" : "envId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "object",
                  "additionalProperties" : {
                    "type" : "string"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "List the tables of an environment",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/environments/{environmentId}/systems/lazy" : {
      "get" : {
        "operationId" : "getLazySystems",
        "parameters" : [
          {
            "description" : "Environment ID.",
            "in" : "path",
            "name" : "environmentId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/LazySystem"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "List the systems of an environment",
        "tags" : [
          "environments-controller"
        ]
      }
    },
    "/api/tdm/envs/reset/caches" : {
      "get" : {
        "description" : "Does nothing and returns true.",
        "operationId" : "resetCaches",
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "boolean"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Reset the environment caches",
        "tags" : [
          "environments-controller"
        ]
      }
    },
    "/api/tdm/evaluate/query" : {
      "put" : {
        "description" : "Replaces the ${...} macros in the query from the request body with values from the table, and returns the result in the query field.",
        "operationId" : "evaluateQuery",
        "parameters" : [
          {
            "description" : "Database table name of the test data table.",
            "in" : "query",
            "name" : "tableName",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          }
        ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "type" : "string",
                "description" : "Query with macros."
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "object",
                  "additionalProperties" : {
                    "type" : "string"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Substitute table values into a query",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/fill/envId" : {
      "get" : {
        "description" : "Sets the environment ID in the catalog entries of tables that have a system. Deletes the tables of a project whose loading fails with a \"not found\" error. A migration for tables created before the catalog stored the environment.",
        "operationId" : "fillEnvIdColumn",
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        },
        "summary" : "Fill in the environment of existing tables",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/fix/occupied/by/column" : {
      "get" : {
        "description" : "Adds the OCCUPIED_BY column to every test data table that lacks it. A migration for tables created before the column existed.",
        "operationId" : "alterOccupiedByColumn",
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        },
        "summary" : "Add the OCCUPIED_BY column to all tables",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/import/excel" : {
      "post" : {
        "description" : "Adds the rows of the file to the table with this title in the system, or creates the table. environmentId and systemId are required: without them the import fails.",
        "operationId" : "importExcelTestData",
        "parameters" : [
          {
            "description" : "Project ID.",
            "in" : "query",
            "name" : "projectId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "Environment ID.",
            "in" : "query",
            "name" : "environmentId",
            "required" : false,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "System ID.",
            "in" : "query",
            "name" : "systemId",
            "required" : false,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "Title of the table.",
            "in" : "query",
            "name" : "tableTitle",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          },
          {
            "description" : "After the import, updates the rows with the SQL query saved for the table.",
            "in" : "query",
            "name" : "runSqlScript",
            "required" : true,
            "schema" : {
              "type" : "boolean"
            }
          }
        ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "type" : "object",
                "properties" : {
                  "file" : {
                    "type" : "string",
                    "format" : "binary",
                    "description" : "Excel file with the rows."
                  }
                },
                "required" : [
                  "file"
                ]
              }
            }
          }
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/ImportTestDataStatistic"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Import rows from an Excel file",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/import/sql" : {
      "post" : {
        "description" : "For each environment, runs the query against the database of the system with this name, and adds the result to the table with this title, or creates the table. The query and timeout are saved for refresh. Returns one import result per environment; a missing environment or system is reported in the result.",
        "operationId" : "importSqlTestData",
        "parameters" : [
          {
            "description" : "Project ID.",
            "in" : "query",
            "name" : "projectId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "IDs of the environments to import from.",
            "in" : "query",
            "name" : "environmentsIds",
            "required" : true,
            "schema" : {
              "type" : "array",
              "items" : {
                "type" : "string",
                "format" : "uuid"
              }
            }
          },
          {
            "description" : "Name of the system whose DB connection runs the query.",
            "in" : "query",
            "name" : "systemName",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          },
          {
            "description" : "Title of the table.",
            "in" : "query",
            "name" : "tableTitle",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          },
          {
            "description" : "SQL query that returns the rows.",
            "in" : "query",
            "name" : "query",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          },
          {
            "description" : "Query timeout, in seconds.",
            "in" : "query",
            "name" : "queryTimeout",
            "required" : true,
            "schema" : {
              "type" : "integer",
              "format" : "int32"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/ImportTestDataStatistic"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Import rows with an SQL query",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/link/preview" : {
      "post" : {
        "description" : "Returns, as a JSON string, the link that setup would build for the column: the value of the first row of the column when pickUpFullLinkFromTableCell is set, otherwise the url parameter of the system HTTP connection joined with the endpoint from the request body.",
        "operationId" : "getPreviewLink",
        "parameters" : [
          {
            "description" : "Project ID.",
            "in" : "query",
            "name" : "projectId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "System ID.",
            "in" : "query",
            "name" : "systemId",
            "required" : false,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "Name of the column.",
            "in" : "query",
            "name" : "columnName",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          },
          {
            "description" : "Database table name; used with pickUpFullLinkFromTableCell.",
            "in" : "query",
            "name" : "tableName",
            "required" : false,
            "schema" : {
              "type" : "string"
            }
          },
          {
            "description" : "Takes the whole link from the column value instead of building it.",
            "in" : "query",
            "name" : "pickUpFullLinkFromTableCell",
            "required" : true,
            "schema" : {
              "type" : "boolean"
            }
          }
        ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "type" : "string",
                "description" : "Endpoint to join with the url parameter of the system HTTP connection."
              }
            }
          }
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "string"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Preview the link of a column",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/link/setup" : {
      "post" : {
        "description" : "Saves the link settings for the column of one table, or, with isAll, of the tables with the same title in all environments of the project.",
        "operationId" : "setupColumnLinks",
        "parameters" : [
          {
            "description" : "Applies the settings to all tables with the same title.",
            "in" : "query",
            "name" : "isAll",
            "required" : true,
            "schema" : {
              "type" : "boolean"
            }
          },
          {
            "description" : "Project ID.",
            "in" : "query",
            "name" : "projectId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "System ID.",
            "in" : "query",
            "name" : "systemId",
            "required" : false,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "Database table name of the test data table.",
            "in" : "query",
            "name" : "tableName",
            "required" : false,
            "schema" : {
              "type" : "string"
            }
          },
          {
            "description" : "Name of the column.",
            "in" : "query",
            "name" : "columnName",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          },
          {
            "description" : "Value of the validate-unoccupied-resources flag saved for the table.",
            "in" : "query",
            "name" : "validateUnoccupiedResources",
            "required" : true,
            "schema" : {
              "type" : "boolean"
            }
          },
          {
            "description" : "Takes the whole link from the column value instead of building it.",
            "in" : "query",
            "name" : "pickUpFullLinkFromTableCell",
            "required" : true,
            "schema" : {
              "type" : "boolean"
            }
          }
        ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "type" : "string",
                "description" : "Endpoint to join with the url parameter of the system HTTP connection."
              }
            }
          }
        },
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        },
        "summary" : "Turn the values of a column into links",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/occupy" : {
      "put" : {
        "description" : "Marks the rows as occupied by the user in occupiedBy and records the occupation in the statistics.",
        "operationId" : "occupyTestData",
        "parameters" : [
          {
            "description" : "Database table name of the test data table.",
            "in" : "query",
            "name" : "tableName",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          },
          {
            "description" : "Name of the user who occupies the rows.",
            "in" : "query",
            "name" : "occupiedBy",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          }
        ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "type" : "array",
                "description" : "ROW_ID values of the rows.",
                "items" : {
                  "type" : "string",
                  "format" : "uuid"
                }
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        },
        "summary" : "Occupy rows by ID",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/projects/lazy" : {
      "get" : {
        "description" : "Returns the ID and name of each project in PROJECTS_INFO.",
        "operationId" : "getProjects",
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/LazyProject"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "List projects",
        "tags" : [
          "environments-controller"
        ]
      }
    },
    "/api/tdm/projects/{projectId}/environments/lazy" : {
      "get" : {
        "description" : "Returns each environment of the project with the IDs of its systems.",
        "operationId" : "getLazyEnvironments",
        "parameters" : [
          {
            "description" : "Project ID.",
            "in" : "path",
            "name" : "projectId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/LazyEnvironment"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "List the environments of a project",
        "tags" : [
          "environments-controller"
        ]
      }
    },
    "/api/tdm/projects/{projectId}/environments/lazy/refresh" : {
      "get" : {
        "description" : "Returns the same list as GET /api/tdm/projects/{projectId}/environments/lazy. Environments are read from the database on each request.",
        "operationId" : "getLazyEnvironmentsRefresh",
        "parameters" : [
          {
            "description" : "Project ID.",
            "in" : "path",
            "name" : "projectId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/LazyEnvironment"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Reload the environments of a project",
        "tags" : [
          "environments-controller"
        ]
      }
    },
    "/api/tdm/release" : {
      "put" : {
        "description" : "Makes the rows available again and removes their occupation records from the statistics.",
        "operationId" : "releaseTestData",
        "parameters" : [
          {
            "description" : "Database table name of the test data table.",
            "in" : "query",
            "name" : "tableName",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          }
        ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "type" : "array",
                "description" : "ROW_ID values of the rows.",
                "items" : {
                  "type" : "string",
                  "format" : "uuid"
                }
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        },
        "summary" : "Release rows by ID",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/resolve/discrepancy/testDataFlagsTableAndTestDataTableCatalog" : {
      "get" : {
        "description" : "Adds default validation flags for catalog tables that have none, and deletes flags of tables that are not in the catalog.",
        "operationId" : "resolveDiscrepancyTestDataFlagsTableAndTestDataTableCatalog",
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        },
        "summary" : "Align the flags table with the catalog",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/rest/add-info-to-row" : {
      "post" : {
        "description" : "For each request in add-info-to-row-requests, appends each value in record-with-data-for-update on a new line to the current value of its column, in every row that matches search-row-parameters-set, and returns the number of updated rows.",
        "operationId" : "addInfoToRow",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RestApiRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/ResponseMessage"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Add values to rows",
        "tags" : [
          "atp-action-controller"
        ]
      }
    },
    "/api/tdm/rest/create-env" : {
      "delete" : {
        "description" : "Deletes the system in systemDeleteName, or the whole environment when systemDeleteName is not set. Returns HTTP 404 when the environment does not exist.",
        "operationId" : "deleteEnvironment",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/EnvironmentManagementRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ResponseMessage"
                }
              }
            },
            "description" : "OK"
          },
          "404" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ResponseMessage"
                }
              }
            },
            "description" : "Not Found"
          }
        },
        "summary" : "Delete an environment or a system",
        "tags" : [
          "atp-env-controller"
        ]
      },
      "post" : {
        "description" : "Creates the environment with the system and its connection, or adds the system to an existing environment. Returns HTTP 400 when the system already exists.",
        "operationId" : "createEnvironment",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/EnvironmentManagementRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ResponseMessage"
                }
              }
            },
            "description" : "OK"
          },
          "404" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ResponseMessage"
                }
              }
            },
            "description" : "Not Found"
          }
        },
        "summary" : "Create an environment or add a system",
        "tags" : [
          "atp-env-controller"
        ]
      },
      "put" : {
        "description" : "Replaces the connection of the system, and renames the environment to newEnvName or the system to newSystemName when they are set.",
        "operationId" : "updateEnvironment",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/EnvironmentManagementRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ResponseMessage"
                }
              }
            },
            "description" : "OK"
          },
          "404" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ResponseMessage"
                }
              }
            },
            "description" : "Not Found"
          }
        },
        "summary" : "Update a connection, or rename an environment or system",
        "tags" : [
          "atp-env-controller"
        ]
      }
    },
    "/api/tdm/rest/get-record" : {
      "post" : {
        "description" : "For each request in get-row-requests, returns the value of name-column-response from the first available row that matches search-row-parameters-set. The row is not occupied.",
        "operationId" : "atpGetTestData",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RestApiRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/ResponseMessage"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Read one column of a row",
        "tags" : [
          "atp-action-controller"
        ]
      }
    },
    "/api/tdm/rest/get-records" : {
      "post" : {
        "description" : "For each request in get-row-requests, returns the values of response-column-names from the first available row that matches search-row-parameters-set, as a JSON object. The row is not occupied.",
        "operationId" : "getMultipleColumnTestData",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RestApiRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/ResponseMessage"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Read several columns of a row",
        "tags" : [
          "atp-action-controller"
        ]
      }
    },
    "/api/tdm/rest/insert-records" : {
      "post" : {
        "description" : "Inserts the rows from insert-records. When no table with this title exists, creates the table first.",
        "operationId" : "insertTestData",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RestApiRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ResponseMessage"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Insert rows into a table",
        "tags" : [
          "atp-action-controller"
        ]
      }
    },
    "/api/tdm/rest/occupy-records" : {
      "post" : {
        "description" : "For each request in occupy-row-requests, occupies the first available row that matches search-row-parameters-set and returns the value of name-column-response. The row is marked as occupied by ATP_User.",
        "operationId" : "atpOccupyTestData",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RestApiRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/ResponseMessage"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Occupy rows and return one column",
        "tags" : [
          "atp-action-controller"
        ]
      }
    },
    "/api/tdm/rest/occupy-records-full-row" : {
      "post" : {
        "description" : "For each request in occupy-full-row-requests, occupies the first available row that matches search-row-parameters-set and returns the values of response-column-names as a JSON object. The row stays available when one of the columns does not exist. The row is marked as occupied by ATP_User.",
        "operationId" : "occupyTestDataFullRow",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RestApiRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/ResponseMessage"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Occupy rows and return several columns",
        "tags" : [
          "atp-action-controller"
        ]
      }
    },
    "/api/tdm/rest/refresh-tables" : {
      "post" : {
        "description" : "Runs the refresh query of the table with this title in the system, or of every table with this title in the project when envName or systemName is missing.",
        "operationId" : "refreshTables",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RestApiRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/ResponseMessage"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Refresh tables by title",
        "tags" : [
          "atp-action-controller"
        ]
      }
    },
    "/api/tdm/rest/release-records" : {
      "post" : {
        "description" : "For each request in release-row-requests, releases the occupied row that matches search-row-parameters-set and returns the value of name-column-response. The request fails when more than one occupied row matches.",
        "operationId" : "atpReleaseTestData",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RestApiRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/ResponseMessage"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Release occupied rows",
        "tags" : [
          "atp-action-controller"
        ]
      }
    },
    "/api/tdm/rest/release-records/bulk" : {
      "post" : {
        "operationId" : "releaseFullTestData",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RestApiRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/ResponseMessage"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Release all occupied rows of a table",
        "tags" : [
          "atp-action-controller"
        ]
      }
    },
    "/api/tdm/rest/resolve-table" : {
      "post" : {
        "description" : "Returns the name of the database table that stores the table with this title. All four fields are required; a missing field or an unknown table returns HTTP 400.",
        "operationId" : "resolveTableName",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RestApiRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ResponseMessage"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Get the database table name of a table",
        "tags" : [
          "atp-action-controller"
        ]
      }
    },
    "/api/tdm/rest/run-cleanup-table" : {
      "post" : {
        "description" : "Runs the cleanup configured for the table, and returns the number of removed rows. Unlike the other operations, projectName must hold the project ID.",
        "operationId" : "runCleanupForTable",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RestApiRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/ResponseMessage"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Run the cleanup of a table",
        "tags" : [
          "atp-action-controller"
        ]
      }
    },
    "/api/tdm/rest/truncate-table" : {
      "post" : {
        "description" : "Unlike the other operations, projectName must hold the project ID.",
        "operationId" : "truncateTable",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RestApiRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/ResponseMessage"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Delete all rows of a table",
        "tags" : [
          "atp-action-controller"
        ]
      }
    },
    "/api/tdm/rest/update-records" : {
      "post" : {
        "description" : "For each request in update-row-requests, sets the columns in record-with-data-for-update to the given values in every row that matches search-row-parameters-set, and returns the number of updated rows.",
        "operationId" : "updateTestData",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/RestApiRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/ResponseMessage"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Update rows",
        "tags" : [
          "atp-action-controller"
        ]
      }
    },
    "/api/tdm/statistics/available/column/configuration" : {
      "get" : {
        "description" : "Returns the settings of the available-data-by-column statistics for a system in an environment, with the column names of the system tables to choose from.",
        "operationId" : "getAvailableDataStatsConfig",
        "parameters" : [
          {
            "description" : "System ID.",
            "in" : "query",
            "name" : "systemId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "Environment ID.",
            "in" : "query",
            "name" : "environmentId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/AvailableDataStatisticsConfig"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Get the available-data-by-column settings",
        "tags" : [
          "statistics-controller"
        ]
      },
      "post" : {
        "operationId" : "saveAvailableDataStatsConfig",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/AvailableDataStatisticsConfig"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        },
        "summary" : "Save the available-data-by-column settings",
        "tags" : [
          "statistics-controller"
        ]
      }
    },
    "/api/tdm/statistics/data/available" : {
      "get" : {
        "description" : "Returns, for each table, the number of available rows, occupied rows, rows occupied today, and all rows.",
        "operationId" : "getTestDataAvailability",
        "parameters" : [
          {
            "description" : "Project ID.",
            "in" : "query",
            "name" : "projectId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "System ID. When omitted, covers all systems of the project.",
            "in" : "query",
            "name" : "systemId",
            "required" : false,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/GeneralStatisticsItem"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Get available and occupied row counts",
        "tags" : [
          "statistics-controller"
        ]
      }
    },
    "/api/tdm/statistics/data/created/when" : {
      "get" : {
        "description" : "Returns, for each table, how many rows were created in each part of the period. The values are split into days, weeks, months, or years, depending on the length of the period.",
        "operationId" : "getTestDataCreatedWhen",
        "parameters" : [
          {
            "description" : "Project ID.",
            "in" : "query",
            "name" : "projectId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "System ID. When omitted, covers all systems of the project.",
            "in" : "query",
            "name" : "systemId",
            "required" : false,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "First day of the period, as yyyy-MM-dd.",
            "in" : "query",
            "name" : "dateFrom",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          },
          {
            "description" : "Last day of the period, as yyyy-MM-dd.",
            "in" : "query",
            "name" : "dateTo",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/DateStatistics"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Get the number of created rows per period",
        "tags" : [
          "statistics-controller"
        ]
      }
    },
    "/api/tdm/statistics/data/occupied" : {
      "get" : {
        "description" : "Returns, for each table, how many rows were occupied in each part of the period. The values are split into days, weeks, months, or years, depending on the length of the period.",
        "operationId" : "getTestDataConsumption",
        "parameters" : [
          {
            "description" : "Project ID.",
            "in" : "query",
            "name" : "projectId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "System ID. When omitted, covers all systems of the project.",
            "in" : "query",
            "name" : "systemId",
            "required" : false,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "First day of the period, as yyyy-MM-dd.",
            "in" : "query",
            "name" : "dateFrom",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          },
          {
            "description" : "Last day of the period, as yyyy-MM-dd.",
            "in" : "query",
            "name" : "dateTo",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ConsumedStatistics"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Get the number of occupied rows per period",
        "tags" : [
          "statistics-controller"
        ]
      }
    },
    "/api/tdm/statistics/data/occupied/available" : {
      "get" : {
        "description" : "Returns, for each table of the system, the number of available rows per value of the configured columns. Fails when the settings of this statistics are not saved for the system and environment.",
        "operationId" : "getAvailableData",
        "parameters" : [
          {
            "description" : "System ID.",
            "in" : "query",
            "name" : "systemId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "Environment ID.",
            "in" : "query",
            "name" : "environmentId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/AvailableDataByColumnStats"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Get available rows by column value",
        "tags" : [
          "statistics-controller"
        ]
      }
    },
    "/api/tdm/statistics/data/occupied/users" : {
      "post" : {
        "description" : "Returns a page of occupation records by user for the project and the period in the request body. dateTo must be later than dateFrom; otherwise the request returns HTTP 400.",
        "operationId" : "getStatisticsByUsers",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/UsersOccupyStatisticRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/UsersOccupyStatisticResponse"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Get rows occupied by users",
        "tags" : [
          "statistics-controller"
        ]
      }
    },
    "/api/tdm/statistics/data/outdated" : {
      "get" : {
        "description" : "Returns, for each table, how many rows were created, consumed, and outdated in each part of the period. Rows occupied on or after dateFrom plus expirationDate days are counted as outdated. The values are split into days, weeks, months, or years, depending on the length of the period.",
        "operationId" : "getTestDataConsumptionWhitOutdated",
        "parameters" : [
          {
            "description" : "Project ID.",
            "in" : "query",
            "name" : "projectId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "System ID. When omitted, covers all systems of the project.",
            "in" : "query",
            "name" : "systemId",
            "required" : false,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "First day of the period, as yyyy-MM-dd.",
            "in" : "query",
            "name" : "dateFrom",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          },
          {
            "description" : "Last day of the period, as yyyy-MM-dd.",
            "in" : "query",
            "name" : "dateTo",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          },
          {
            "description" : "Number of days after dateFrom from which occupied rows count as outdated.",
            "in" : "query",
            "name" : "expirationDate",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/OutdatedStatistics"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Get created, consumed, and outdated row counts per period",
        "tags" : [
          "statistics-controller"
        ]
      }
    },
    "/api/tdm/statistics/delete/schedule" : {
      "put" : {
        "description" : "Deletes the schedule of the project in projectId and stops the report. Only projectId is read from the request body.",
        "operationId" : "deleteScheduledRun",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/TestDataTableMonitoring"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        },
        "summary" : "Delete the statistics report schedule",
        "tags" : [
          "statistics-controller"
        ]
      }
    },
    "/api/tdm/statistics/delete/schedule/users" : {
      "put" : {
        "operationId" : "deleteUsersScheduledRun",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/TestDataTableUsersMonitoring"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        },
        "summary" : "Delete the users report schedule",
        "tags" : [
          "statistics-controller"
        ]
      }
    },
    "/api/tdm/statistics/fix/occupied/date/column" : {
      "get" : {
        "description" : "Adds the OCCUPIED_DATE column to every test data table that lacks it, and returns the table names. A migration for tables created before the column existed.",
        "operationId" : "alterOccupiedDateColumn",
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "type" : "string"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Add the OCCUPIED_DATE column to all tables",
        "tags" : [
          "statistics-controller"
        ]
      }
    },
    "/api/tdm/statistics/next/run" : {
      "get" : {
        "description" : "Returns the next time the Quartz cron expression fires after now, in the nextRun field, in \"EEE MMM dd HH:mm:ss zzz yyyy\" format.",
        "operationId" : "getNextStatisticsRun",
        "parameters" : [
          {
            "description" : "Quartz cron expression, starting with the seconds field.",
            "in" : "query",
            "name" : "cronExpression",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "object",
                  "additionalProperties" : {
                    "type" : "string"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Get the next run time of a schedule",
        "tags" : [
          "statistics-controller"
        ]
      }
    },
    "/api/tdm/statistics/schedule" : {
      "get" : {
        "description" : "Returns the schedule, recipients, and threshold of the project statistics report. The report splits the tables into those with at least threshold available rows and those with fewer. Returns a disabled schedule with empty fields when the project has none.",
        "operationId" : "getMonitoringSchedule",
        "parameters" : [
          {
            "description" : "Project ID.",
            "in" : "query",
            "name" : "projectId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/TestDataTableMonitoring"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Get the statistics report schedule",
        "tags" : [
          "statistics-controller"
        ]
      },
      "post" : {
        "description" : "Saves the schedule of the project statistics report and reschedules it. cronExpression is a Quartz cron expression.",
        "operationId" : "setupScheduledRun",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/TestDataTableMonitoring"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        },
        "summary" : "Save the statistics report schedule",
        "tags" : [
          "statistics-controller"
        ]
      }
    },
    "/api/tdm/statistics/schedule/available" : {
      "delete" : {
        "operationId" : "deleteAvailableDataMonitoringConfig",
        "parameters" : [
          {
            "description" : "System ID.",
            "in" : "query",
            "name" : "systemId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "Environment ID.",
            "in" : "query",
            "name" : "environmentId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          }
        ],
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        },
        "summary" : "Delete the available-data report schedule",
        "tags" : [
          "statistics-controller"
        ]
      },
      "get" : {
        "operationId" : "getAvailableDataMonitoringConfig",
        "parameters" : [
          {
            "description" : "System ID.",
            "in" : "query",
            "name" : "systemId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "Environment ID.",
            "in" : "query",
            "name" : "environmentId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/TestAvailableDataMonitoring"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Get the available-data report schedule",
        "tags" : [
          "statistics-controller"
        ]
      },
      "post" : {
        "operationId" : "saveAvailableDataMonitoringConfig",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/TestAvailableDataMonitoring"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        },
        "summary" : "Save the available-data report schedule",
        "tags" : [
          "statistics-controller"
        ]
      }
    },
    "/api/tdm/statistics/schedule/users" : {
      "get" : {
        "description" : "Returns the schedule and settings of the report on rows occupied by users. Returns a disabled schedule with empty fields when the project has none.",
        "operationId" : "getUsersMonitoringSchedule",
        "parameters" : [
          {
            "description" : "Project ID.",
            "in" : "query",
            "name" : "projectId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/TestDataTableUsersMonitoring"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Get the users report schedule",
        "tags" : [
          "statistics-controller"
        ]
      },
      "post" : {
        "operationId" : "setupUsersScheduledRun",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/TestDataTableUsersMonitoring"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        },
        "summary" : "Save the users report schedule",
        "tags" : [
          "statistics-controller"
        ]
      }
    },
    "/api/tdm/statistics/threshold" : {
      "get" : {
        "description" : "Returns the default threshold of available rows for the statistics report: the value of the test.data.initial.threshold property, 10 by default.",
        "operationId" : "getThreshold",
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "integer",
                  "format" : "int32"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Get the default report threshold",
        "tags" : [
          "statistics-controller"
        ]
      }
    },
    "/api/tdm/table" : {
      "delete" : {
        "description" : "Drops the database table, removes it from the catalog, and stops its scheduled refresh.",
        "operationId" : "deleteTestData",
        "parameters" : [
          {
            "description" : "Database table name of the test data table.",
            "in" : "query",
            "name" : "tableName",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          }
        ],
        "responses" : {
          "200" : {
            "description" : "OK"
          }
        },
        "summary" : "Delete a table",
        "tags" : [
          "test-data-controller"
        ]
      },
      "post" : {
        "description" : "Returns a page of rows that match the filters in the request body, sorted as requested, from the available rows or, with occupied set, from the occupied rows.",
        "operationId" : "getTestData",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/TestDataRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/TestDataTable"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Read rows of a table",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/table/column/distinct/values" : {
      "get" : {
        "description" : "Returns the distinct values of the column among the available rows, or among the occupied rows when occupied is true.",
        "operationId" : "getColumnDistinctValues",
        "parameters" : [
          {
            "description" : "Database table name of the test data table.",
            "in" : "query",
            "name" : "tableName",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          },
          {
            "description" : "Name of the column.",
            "in" : "query",
            "name" : "columnName",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          },
          {
            "description" : "Reads the occupied rows instead of the available ones.",
            "in" : "query",
            "name" : "occupied",
            "required" : true,
            "schema" : {
              "type" : "boolean"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ColumnValues"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "List the distinct values of a column",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/table/environments" : {
      "get" : {
        "description" : "Returns the IDs of the environments that have a table with this title in the project.",
        "operationId" : "getTableEnvironments",
        "parameters" : [
          {
            "description" : "Project ID.",
            "in" : "query",
            "name" : "projectId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "Title of the table.",
            "in" : "query",
            "name" : "tableTitle",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/EnvsList"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "List the environments of a table title",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/table/row" : {
      "get" : {
        "description" : "Returns the first row of the table with this title whose column equals searchValue, case-sensitively.",
        "operationId" : "getTableRow",
        "parameters" : [
          {
            "description" : "Project ID.",
            "in" : "query",
            "name" : "projectId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "System ID.",
            "in" : "query",
            "name" : "systemId",
            "required" : false,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "Title of the table.",
            "in" : "query",
            "name" : "tableTitle",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          },
          {
            "description" : "Name of the column to search in.",
            "in" : "query",
            "name" : "columnName",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          },
          {
            "description" : "Value to search for.",
            "in" : "query",
            "name" : "searchValue",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          },
          {
            "description" : "Searches the occupied rows instead of the available ones.",
            "in" : "query",
            "name" : "occupied",
            "required" : false,
            "schema" : {
              "type" : "boolean"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "object",
                  "additionalProperties" : {
                    "type" : "object"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Find a row by column value",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/tables/catalog" : {
      "get" : {
        "description" : "Returns the catalog entries of the tables, with the import query and timeout of tables imported by SQL.",
        "operationId" : "getTestDataTablesCatalog",
        "parameters" : [
          {
            "description" : "Project ID.",
            "in" : "query",
            "name" : "projectId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "System ID. When omitted, lists the tables of all systems.",
            "in" : "query",
            "name" : "systemId",
            "required" : false,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/TestDataTableCatalog"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "List the tables of a project",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/tables/list" : {
      "get" : {
        "description" : "Returns the project ID and the database table names of its tables.",
        "operationId" : "getTestDataTablesList",
        "parameters" : [
          {
            "description" : "Project ID.",
            "in" : "query",
            "name" : "projectId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/TdmDataToExport"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "List the table names of a project",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/truncate/table" : {
      "delete" : {
        "description" : "Returns HTTP 400 when the project, or the system when given, has no table with this name.",
        "operationId" : "truncateDataInTable",
        "parameters" : [
          {
            "description" : "Database table name of the test data table.",
            "in" : "query",
            "name" : "tableName",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          },
          {
            "description" : "Project ID.",
            "in" : "query",
            "name" : "projectId",
            "required" : true,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          },
          {
            "description" : "System ID.",
            "in" : "query",
            "name" : "systemId",
            "required" : false,
            "schema" : {
              "type" : "string",
              "format" : "uuid"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ResponseMessage"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Delete all rows of a table",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/update/sql" : {
      "post" : {
        "description" : "Runs the query against the database of the system and updates the table rows from its result. The query and timeout are saved with the table.",
        "operationId" : "updateTableBySql",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/TestDataTableUpdateByQuery"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/ImportTestDataStatistic"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Update rows with an SQL query",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/v2/import/sql" : {
      "post" : {
        "description" : "Does what POST /api/tdm/import/sql does, with the parameters in the request body. For each environment, runs the query against the database of the system with this name, and adds the result to the table with this title, or creates the table.",
        "operationId" : "importSqlTestDataV2",
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ImportSqlTestDataRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/ImportTestDataStatistic"
                  }
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Import rows with an SQL query",
        "tags" : [
          "test-data-controller-v2"
        ]
      }
    },
    "/api/tdm/validation/unoccupied" : {
      "get" : {
        "operationId" : "isUnoccupiedValidation",
        "parameters" : [
          {
            "description" : "Database table name of the test data table.",
            "in" : "query",
            "name" : "tableName",
            "required" : true,
            "schema" : {
              "type" : "string"
            }
          }
        ],
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/TestDataFlagsTable"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Get the validation flags of a table",
        "tags" : [
          "test-data-controller"
        ]
      }
    },
    "/api/tdm/versions/backend" : {
      "get" : {
        "operationId" : "getBackendVersion",
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/VersionDto"
                }
              }
            },
            "description" : "OK"
          }
        },
        "summary" : "Get the service name and version",
        "tags" : [
          "versions-controller"
        ]
      }
    },
    "/atp-integration/routeInfo" : {
      "get" : {
        "operationId" : "getRouteInfo",
        "responses" : {
          "200" : {
            "content" : {
              "*/*" : {
                "schema" : {
                  "$ref" : "#/components/schemas/AtpRoute"
                }
              }
            },
            "description" : "OK"
          }
        },
        "tags" : [
          "route-info-controller"
        ]
      }
    }
  },
  "components" : {
    "schemas" : {
      "AddInfoToRowRequest" : {
        "type" : "object",
        "properties" : {
          "record-with-data-for-update" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string",
              "description" : "Column names and the values to write to them, in every row that matches the filters."
            },
            "description" : "Column names and the values to write to them, in every row that matches the filters."
          },
          "search-row-parameters-set" : {
            "type" : "array",
            "description" : "Row to act on: every condition must match. The row is found among the available rows, except where the request's own operation reads the occupied rows instead.",
            "items" : {
              "$ref" : "#/components/schemas/ApiDataFilter"
            }
          }
        },
        "description" : "Values to append to the matching rows. Each value in record-with-data-for-update is added on a new line to the current value of its column; the column keeps its old value if it has one."
      },
      "ApiDataFilter" : {
        "type" : "object",
        "properties" : {
          "caseSensitive" : {
            "type" : "boolean",
            "description" : "Matches the column case-sensitively. Ignored by \"From\" and \"To\"."
          },
          "name-column" : {
            "type" : "string",
            "description" : "Name of the column to search in."
          },
          "search-criterion" : {
            "type" : "string",
            "description" : "\"Contains\", \"startWith\", \"Equals\", \"From\", or \"To\", case-insensitive."
          },
          "search-value" : {
            "type" : "string",
            "description" : "Value to compare the column against."
          }
        },
        "description" : "One search condition on a column, used by the ATP action row requests to find the row to act on."
      },
      "AtpRoute" : {
        "type" : "object",
        "properties" : {
          "contextPath" : {
            "type" : "string"
          },
          "internal" : {
            "type" : "boolean"
          },
          "path" : {
            "type" : "string"
          },
          "port" : {
            "type" : "integer",
            "format" : "int32"
          },
          "public" : {
            "type" : "boolean"
          },
          "serviceId" : {
            "type" : "string"
          },
          "url" : {
            "type" : "string"
          }
        }
      },
      "AvailableDataByColumnStats" : {
        "type" : "object",
        "properties" : {
          "description" : {
            "type" : "string",
            "description" : "Name of the column the counts are grouped by."
          },
          "statistics" : {
            "type" : "array",
            "description" : "One entry per table.",
            "items" : {
              "$ref" : "#/components/schemas/TableAvailableDataStats"
            }
          }
        },
        "description" : "Number of available rows of each table for each value of a chosen column."
      },
      "AvailableDataStatisticsConfig" : {
        "type" : "object",
        "properties" : {
          "activeColumnKey" : {
            "type" : "string",
            "description" : "Same value as description: the column currently grouped on."
          },
          "columnKeys" : {
            "type" : "array",
            "description" : "Names of the columns available to group on, common to every table of the system.",
            "items" : {
              "type" : "string",
              "description" : "Names of the columns available to group on, common to every table of the system."
            }
          },
          "description" : {
            "type" : "string",
            "description" : "Name of the column currently grouped on; one of columnKeys."
          },
          "environmentId" : {
            "type" : "string",
            "description" : "Environment ID.",
            "format" : "uuid"
          },
          "systemId" : {
            "type" : "string",
            "description" : "System ID.",
            "format" : "uuid"
          },
          "tablesColumns" : {
            "type" : "array",
            "description" : "Per-table filter: for each table, the column values to count as available. A table missing here counts every value.",
            "items" : {
              "$ref" : "#/components/schemas/TableColumnValues"
            }
          }
        },
        "description" : "Available-data-by-column monitoring configuration of one system's environment."
      },
      "ChangeTitleRequest" : {
        "type" : "object",
        "properties" : {
          "tableName" : {
            "type" : "string",
            "description" : "Database table name of the table to rename."
          },
          "tableTitle" : {
            "type" : "string",
            "description" : "New title for the table."
          }
        },
        "description" : "Request body of the rename-table operation."
      },
      "CleanupResults" : {
        "type" : "object",
        "properties" : {
          "error" : {
            "type" : "string",
            "description" : "Set instead of the record counts when the cleanup failed."
          },
          "recordsRemoved" : {
            "type" : "integer",
            "description" : "Number of rows the cleanup removed.",
            "format" : "int32"
          },
          "recordsTotal" : {
            "type" : "integer",
            "description" : "Number of rows in the table before the cleanup.",
            "format" : "int32"
          },
          "tableName" : {
            "type" : "string",
            "description" : "Database table name of the table the cleanup ran on."
          }
        },
        "description" : "Result of one cleanup run, for one table."
      },
      "CleanupSettings" : {
        "type" : "object",
        "properties" : {
          "environmentsList" : {
            "type" : "array",
            "description" : "IDs of the environments of the tables that already share this configuration, in the response of GET /api/tdm/cleanup/config/{id}; the environments to share it with, when saving.",
            "items" : {
              "type" : "string",
              "description" : "IDs of the environments of the tables that already share this configuration, in the response of GET /api/tdm/cleanup/config/{id}; the environments to share it with, when saving.",
              "format" : "uuid"
            }
          },
          "tableName" : {
            "type" : "string",
            "description" : "Database table name of the table to save the configuration for. Read only when saving; not set in the response of GET /api/tdm/cleanup/config/{id}."
          },
          "testDataCleanupConfig" : {
            "$ref" : "#/components/schemas/TestDataCleanupConfig"
          }
        },
        "description" : "Cleanup settings of a table, and, when saving them, the environments of the other tables with the same title and system to apply them to too."
      },
      "ColumnValues" : {
        "type" : "object",
        "properties" : {
          "items" : {
            "type" : "array",
            "description" : "The distinct values.",
            "items" : {
              "type" : "string",
              "description" : "The distinct values."
            }
          }
        },
        "description" : "The distinct values found in one column of a table."
      },
      "ConsumedStatistics" : {
        "type" : "object",
        "properties" : {
          "dates" : {
            "type" : "array",
            "description" : "Label of each part of the period. items.consumed has one value per label, in the same order.",
            "items" : {
              "type" : "string",
              "description" : "Label of each part of the period. items.consumed has one value per label, in the same order."
            }
          },
          "items" : {
            "type" : "array",
            "description" : "One entry per table.",
            "items" : {
              "$ref" : "#/components/schemas/ConsumedStatisticsItem"
            }
          }
        },
        "description" : "Number of rows occupied in each part of the requested period, for every table."
      },
      "ConsumedStatisticsItem" : {
        "type" : "object",
        "properties" : {
          "consumed" : {
            "type" : "array",
            "description" : "Number of rows occupied in each part of the period, one value per label of ConsumedStatistics.dates, in the same order.",
            "items" : {
              "type" : "integer",
              "description" : "Number of rows occupied in each part of the period, one value per label of ConsumedStatistics.dates, in the same order.",
              "format" : "int64"
            }
          },
          "context" : {
            "type" : "string",
            "description" : "Title of the table."
          },
          "details" : {
            "type" : "array",
            "description" : "Set instead of consumed on a row grouped across systems: one entry per system, with consumed set.",
            "items" : {
              "$ref" : "#/components/schemas/ConsumedStatisticsItem"
            }
          },
          "environment" : {
            "type" : "string",
            "description" : "Environment name, or \"across all\" for a row that sums every environment of the project."
          },
          "system" : {
            "type" : "string",
            "description" : "System name, or \"across all\" for a row that sums every system of the environment."
          }
        },
        "description" : "Number of rows of one table occupied in each part of the requested period."
      },
      "DateStatistics" : {
        "type" : "object",
        "properties" : {
          "dates" : {
            "type" : "array",
            "description" : "Label of each part of the period. items.created has one value per label, in the same order.",
            "items" : {
              "type" : "string",
              "description" : "Label of each part of the period. items.created has one value per label, in the same order."
            }
          },
          "items" : {
            "type" : "array",
            "description" : "One entry per table.",
            "items" : {
              "$ref" : "#/components/schemas/DateStatisticsItem"
            }
          }
        },
        "description" : "Number of rows created in each part of the requested period, for every table."
      },
      "DateStatisticsItem" : {
        "type" : "object",
        "properties" : {
          "context" : {
            "type" : "string",
            "description" : "Title of the table."
          },
          "created" : {
            "type" : "array",
            "description" : "Number of rows created in each part of the period, one value per label of DateStatistics.dates, in the same order.",
            "items" : {
              "type" : "integer",
              "description" : "Number of rows created in each part of the period, one value per label of DateStatistics.dates, in the same order.",
              "format" : "int64"
            }
          },
          "details" : {
            "type" : "array",
            "description" : "Set instead of created on a row grouped across systems: one entry per system, with created set.",
            "items" : {
              "$ref" : "#/components/schemas/DateStatisticsItem"
            }
          },
          "environment" : {
            "type" : "string",
            "description" : "Environment name, or \"across all\" for a row that sums every environment of the project."
          },
          "system" : {
            "type" : "string",
            "description" : "System name, or \"across all\" for a row that sums every system of the environment."
          }
        },
        "description" : "Number of rows of one table created in each part of the requested period."
      },
      "EnvironmentConnectionRequest" : {
        "type" : "object",
        "properties" : {
          "name" : {
            "type" : "string",
            "description" : "Connection name."
          },
          "parameters" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string",
              "description" : "Connection parameters, specific to type: for example url, username, and password for a DB connection."
            },
            "description" : "Connection parameters, specific to type: for example url, username, and password for a DB connection."
          },
          "type" : {
            "type" : "string",
            "description" : "Connection type, such as \"DB\" or \"HTTP\". See the supported connection types in the atp-env-controller documentation."
          }
        },
        "description" : "Connection details for systemName. Read by POST and PUT."
      },
      "EnvironmentManagementRequest" : {
        "type" : "object",
        "properties" : {
          "connection" : {
            "$ref" : "#/components/schemas/EnvironmentConnectionRequest"
          },
          "envName" : {
            "type" : "string",
            "description" : "Environment name to create, update, or delete."
          },
          "environment" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "object"
            },
            "writeOnly" : true
          },
          "newEnvName" : {
            "type" : "string",
            "description" : "New environment name. Read by PUT to rename the environment."
          },
          "newSystemName" : {
            "type" : "string",
            "description" : "New system name. Read by PUT to rename systemName."
          },
          "projectName" : {
            "type" : "string",
            "description" : "Project name from PROJECTS_INFO."
          },
          "systemDeleteName" : {
            "type" : "string",
            "description" : "Read by DELETE: when set, only this system is deleted; otherwise the whole environment is deleted."
          },
          "systemName" : {
            "type" : "string",
            "description" : "System name within the environment. Read by POST and PUT."
          }
        },
        "description" : "Request body of every atp-env-controller operation (POST, PUT, and DELETE /api/tdm/rest/create-env). Its fields can be sent flat, as shown here, or nested under an \"environment\" property; both are equivalent."
      },
      "EnvsList" : {
        "type" : "object",
        "properties" : {
          "items" : {
            "type" : "array",
            "description" : "Environment IDs.",
            "items" : {
              "type" : "string",
              "description" : "Environment IDs.",
              "format" : "uuid"
            }
          }
        },
        "description" : "A list of environment IDs."
      },
      "GeneralStatisticsItem" : {
        "type" : "object",
        "properties" : {
          "available" : {
            "type" : "integer",
            "description" : "Number of available rows.",
            "format" : "int64"
          },
          "context" : {
            "type" : "string",
            "description" : "Title of the table."
          },
          "details" : {
            "type" : "array",
            "description" : "Set instead of available, occupied, occupiedToday, and total on a row grouped across systems: one entry per system, with those four fields set.",
            "items" : {
              "$ref" : "#/components/schemas/GeneralStatisticsItem"
            }
          },
          "environment" : {
            "type" : "string",
            "description" : "Environment name, or \"across all\" for a row that sums every environment of the project."
          },
          "occupied" : {
            "type" : "integer",
            "description" : "Number of occupied rows.",
            "format" : "int64"
          },
          "occupiedToday" : {
            "type" : "integer",
            "description" : "Number of rows occupied today.",
            "format" : "int64"
          },
          "system" : {
            "type" : "string",
            "description" : "System name, or \"across all\" for a row that sums every system of the environment."
          },
          "total" : {
            "type" : "integer",
            "description" : "Number of rows, available and occupied.",
            "format" : "int64"
          }
        },
        "description" : "Available and occupied row counts of one table. A row grouped across systems carries the per-system rows in details instead of available, occupied, occupiedToday, and total."
      },
      "GetRowRequest" : {
        "type" : "object",
        "properties" : {
          "name-column-response" : {
            "type" : "string",
            "description" : "Name of the column whose value the response returns. Read by POST /api/tdm/rest/get-record."
          },
          "response-column-names" : {
            "type" : "array",
            "description" : "Names of the columns whose values the response returns. Read by POST /api/tdm/rest/get-records.",
            "items" : {
              "type" : "string",
              "description" : "Names of the columns whose values the response returns. Read by POST /api/tdm/rest/get-records."
            }
          },
          "search-row-parameters-set" : {
            "type" : "array",
            "description" : "Row to act on: every condition must match. The row is found among the available rows, except where the request's own operation reads the occupied rows instead.",
            "items" : {
              "$ref" : "#/components/schemas/ApiDataFilter"
            }
          }
        },
        "description" : "Reads the first available row that matches the filters, without occupying it. POST /api/tdm/rest/get-record reads nameColumnResponse; POST /api/tdm/rest/get-records reads responseColumnNames; each endpoint ignores the other field."
      },
      "ImportSqlTestDataRequest" : {
        "type" : "object",
        "properties" : {
          "environmentsIds" : {
            "type" : "array",
            "description" : "Environment IDs to import into; one table is imported or updated per environment.",
            "items" : {
              "type" : "string",
              "description" : "Environment IDs to import into; one table is imported or updated per environment.",
              "format" : "uuid"
            }
          },
          "projectId" : {
            "type" : "string",
            "description" : "Project ID.",
            "format" : "uuid"
          },
          "query" : {
            "type" : "string",
            "description" : "SQL query that returns the rows to import."
          },
          "queryTimeout" : {
            "type" : "integer",
            "description" : "Query timeout, in seconds.",
            "format" : "int32"
          },
          "systemName" : {
            "type" : "string",
            "description" : "System name whose DB connection runs the query."
          },
          "tableTitle" : {
            "type" : "string",
            "description" : "Title of the table to create or update."
          }
        },
        "description" : "Request body of POST /api/tdm/v2: runs a query against a system and imports its result as a new test data table, or as new rows of an existing one with the same title, for each listed environment."
      },
      "ImportTestDataStatistic" : {
        "type" : "object",
        "properties" : {
          "envName" : {
            "type" : "string",
            "description" : "Environment name the import ran on."
          },
          "error" : {
            "type" : "string",
            "description" : "Set instead of processedRows when the import failed for this environment."
          },
          "processedRows" : {
            "type" : "integer",
            "description" : "Number of rows imported.",
            "format" : "int32"
          }
        },
        "description" : "Result of an SQL import into one environment."
      },
      "LazyEnvironment" : {
        "type" : "object",
        "properties" : {
          "clusterName" : {
            "type" : "string",
            "description" : "Cluster name."
          },
          "created" : {
            "type" : "string",
            "description" : "When the environment was created."
          },
          "createdBy" : {
            "type" : "string",
            "description" : "Who created the environment."
          },
          "description" : {
            "type" : "string",
            "description" : "Environment description."
          },
          "id" : {
            "type" : "string",
            "description" : "Environment ID.",
            "format" : "uuid"
          },
          "modified" : {
            "type" : "string",
            "description" : "When the environment was last modified."
          },
          "modifiedBy" : {
            "type" : "string",
            "description" : "Who last modified the environment."
          },
          "name" : {
            "type" : "string",
            "description" : "Environment name."
          },
          "projectId" : {
            "type" : "string",
            "description" : "Project ID.",
            "format" : "uuid"
          },
          "systems" : {
            "type" : "array",
            "description" : "Names of the environment's systems.",
            "items" : {
              "type" : "string",
              "description" : "Names of the environment's systems."
            }
          }
        },
        "description" : "A dynamic environment."
      },
      "LazyProject" : {
        "type" : "object",
        "properties" : {
          "id" : {
            "type" : "string",
            "description" : "Project ID.",
            "format" : "uuid"
          },
          "name" : {
            "type" : "string",
            "description" : "Project name."
          }
        },
        "description" : "A project."
      },
      "LazySystem" : {
        "type" : "object",
        "properties" : {
          "connections" : {
            "type" : "array",
            "description" : "Names of the system's connections.",
            "items" : {
              "type" : "string",
              "description" : "Names of the system's connections."
            }
          },
          "created" : {
            "type" : "string",
            "description" : "When the system was created."
          },
          "createdBy" : {
            "type" : "string",
            "description" : "Who created the system."
          },
          "description" : {
            "type" : "string",
            "description" : "System description."
          },
          "environmentIds" : {
            "type" : "array",
            "description" : "IDs of the environments the system belongs to.",
            "items" : {
              "type" : "string",
              "description" : "IDs of the environments the system belongs to.",
              "format" : "uuid"
            }
          },
          "id" : {
            "type" : "string",
            "description" : "System ID.",
            "format" : "uuid"
          },
          "modified" : {
            "type" : "string",
            "description" : "When the system was last modified."
          },
          "modifiedBy" : {
            "type" : "string",
            "description" : "Who last modified the system."
          },
          "name" : {
            "type" : "string",
            "description" : "System name."
          }
        },
        "description" : "A system of a dynamic environment."
      },
      "OccupiedDataByUsersStatistics" : {
        "type" : "object",
        "properties" : {
          "context" : {
            "type" : "string",
            "description" : "Title of the table."
          },
          "data" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "integer",
              "description" : "Number of rows occupied on each date.",
              "format" : "int64"
            },
            "description" : "Number of rows occupied on each date."
          },
          "environment" : {
            "type" : "string",
            "description" : "Environment name, or \"across all\" for a row that sums every environment of the project."
          },
          "system" : {
            "type" : "string",
            "description" : "System name, or \"across all\" for a row that sums every system of the environment."
          },
          "tableName" : {
            "type" : "string",
            "description" : "Database table name of the table."
          },
          "userName" : {
            "type" : "string",
            "description" : "Name of the user who occupied the rows."
          }
        },
        "description" : "Rows one user occupied in one table, one entry per occupation date."
      },
      "OccupyFullRowRequest" : {
        "type" : "object",
        "properties" : {
          "response-column-names" : {
            "type" : "array",
            "description" : "Names of the columns whose values the response returns. The row stays available if one of them does not exist.",
            "items" : {
              "type" : "string",
              "description" : "Names of the columns whose values the response returns. The row stays available if one of them does not exist."
            }
          },
          "search-row-parameters-set" : {
            "type" : "array",
            "description" : "Row to act on: every condition must match. The row is found among the available rows, except where the request's own operation reads the occupied rows instead.",
            "items" : {
              "$ref" : "#/components/schemas/ApiDataFilter"
            }
          }
        },
        "description" : "Occupies the first available row that matches the filters, and returns the values of several of its columns."
      },
      "OccupyRowRequest" : {
        "type" : "object",
        "properties" : {
          "name-column-response" : {
            "type" : "string",
            "description" : "Name of the column whose value the response returns."
          },
          "search-row-parameters-set" : {
            "type" : "array",
            "description" : "Row to act on: every condition must match. The row is found among the available rows, except where the request's own operation reads the occupied rows instead.",
            "items" : {
              "$ref" : "#/components/schemas/ApiDataFilter"
            }
          }
        },
        "description" : "Occupies the first available row that matches the filters, and returns the value of one of its columns."
      },
      "OutdatedStatistics" : {
        "type" : "object",
        "properties" : {
          "dates" : {
            "type" : "array",
            "description" : "Label of each part of the period. items.created, items.consumed, and items.outdated each have one value per label, in the same order.",
            "items" : {
              "type" : "string",
              "description" : "Label of each part of the period. items.created, items.consumed, and items.outdated each have one value per label, in the same order."
            }
          },
          "items" : {
            "type" : "array",
            "description" : "One entry per table.",
            "items" : {
              "$ref" : "#/components/schemas/OutdatedStatisticsItem"
            }
          }
        },
        "description" : "Number of rows created, consumed, and outdated in each part of the requested period, for every table."
      },
      "OutdatedStatisticsItem" : {
        "type" : "object",
        "properties" : {
          "consumed" : {
            "type" : "array",
            "description" : "Number of rows occupied in each part of the period, in the same order.",
            "items" : {
              "type" : "integer",
              "description" : "Number of rows occupied in each part of the period, in the same order.",
              "format" : "int64"
            }
          },
          "context" : {
            "type" : "string",
            "description" : "Title of the table."
          },
          "created" : {
            "type" : "array",
            "description" : "Number of rows created in each part of the period, one value per label of OutdatedStatistics.dates, in the same order.",
            "items" : {
              "type" : "integer",
              "description" : "Number of rows created in each part of the period, one value per label of OutdatedStatistics.dates, in the same order.",
              "format" : "int64"
            }
          },
          "details" : {
            "type" : "array",
            "description" : "Set instead of created, consumed, and outdated on a row grouped across systems: one entry per system, with those three fields set.",
            "items" : {
              "$ref" : "#/components/schemas/OutdatedStatisticsItem"
            }
          },
          "environment" : {
            "type" : "string",
            "description" : "Environment name, or \"across all\" for a row that sums every environment of the project."
          },
          "outdated" : {
            "type" : "array",
            "description" : "Number of rows occupied on or after expirationDate days past the start of the period, in the same order.",
            "items" : {
              "type" : "integer",
              "description" : "Number of rows occupied on or after expirationDate days past the start of the period, in the same order.",
              "format" : "int64"
            }
          },
          "system" : {
            "type" : "string",
            "description" : "System name, or \"across all\" for a row that sums every system of the environment."
          }
        },
        "description" : "Number of rows of one table created, consumed, and outdated in each part of the requested period."
      },
      "RefreshResults" : {
        "type" : "object",
        "properties" : {
          "recordsTotal" : {
            "type" : "integer",
            "description" : "Number of rows the table has after the refresh.",
            "format" : "int32"
          }
        },
        "description" : "Result of one refresh run, for one table."
      },
      "ReleaseRowRequest" : {
        "type" : "object",
        "properties" : {
          "name-column-response" : {
            "type" : "string",
            "description" : "Name of the column whose value the response returns."
          },
          "search-row-parameters-set" : {
            "type" : "array",
            "description" : "Row to act on: every condition must match. The row is found among the available rows, except where the request's own operation reads the occupied rows instead.",
            "items" : {
              "$ref" : "#/components/schemas/ApiDataFilter"
            }
          }
        },
        "description" : "Releases the occupied row that matches the filters, and returns the value of one of its columns. Fails when more than one occupied row matches."
      },
      "ResponseMessage" : {
        "type" : "object",
        "properties" : {
          "content" : {
            "type" : "string",
            "description" : "A message: the result on success, or the reason for the failure."
          },
          "contentObject" : {
            "type" : "object",
            "description" : "A structured result, set instead of or in addition to content by some operations."
          },
          "link" : {
            "type" : "string",
            "description" : "URL of the affected table or environment in the TDM UI, or an empty string."
          },
          "type" : {
            "type" : "string",
            "description" : "Whether the operation succeeded.",
            "enum" : [
              "SUCCESS",
              "ERROR"
            ]
          }
        },
        "description" : "Outcome of an operation, on success or failure alike. Returned by the ATP action operations and by the Dynamic Environment API."
      },
      "RestApiRequest" : {
        "type" : "object",
        "properties" : {
          "add-info-to-row-requests" : {
            "type" : "array",
            "description" : "Read by /add-info-to-row.",
            "items" : {
              "$ref" : "#/components/schemas/AddInfoToRowRequest"
            }
          },
          "envName" : {
            "type" : "string",
            "description" : "Environment name. A trailing timestamp such as \" 2024-05-01T10:15:30\" is ignored. When missing, the first table with this title in the project is used."
          },
          "environment" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "object"
            },
            "writeOnly" : true
          },
          "get-row-requests" : {
            "type" : "array",
            "description" : "Read by /get-record and /get-records.",
            "items" : {
              "$ref" : "#/components/schemas/GetRowRequest"
            }
          },
          "insert-records" : {
            "type" : "array",
            "description" : "Rows to insert, one map of column name to value per row. Read by /insert-records.",
            "items" : {
              "type" : "object",
              "additionalProperties" : {
                "type" : "object",
                "description" : "Rows to insert, one map of column name to value per row. Read by /insert-records."
              },
              "description" : "Rows to insert, one map of column name to value per row. Read by /insert-records."
            }
          },
          "occupy-full-row-requests" : {
            "type" : "array",
            "description" : "Read by /occupy-records-full-row.",
            "items" : {
              "$ref" : "#/components/schemas/OccupyFullRowRequest"
            }
          },
          "occupy-row-requests" : {
            "type" : "array",
            "description" : "Read by /occupy-records.",
            "items" : {
              "$ref" : "#/components/schemas/OccupyRowRequest"
            }
          },
          "projectName" : {
            "type" : "string",
            "description" : "Project name from PROJECTS_INFO."
          },
          "release-row-requests" : {
            "type" : "array",
            "description" : "Read by /release-records.",
            "items" : {
              "$ref" : "#/components/schemas/ReleaseRowRequest"
            }
          },
          "systemName" : {
            "type" : "string",
            "description" : "System name. When missing, the first table with this title in the project is used."
          },
          "title-table" : {
            "type" : "string",
            "description" : "Title of the table."
          },
          "update-row-requests" : {
            "type" : "array",
            "description" : "Read by /update-records.",
            "items" : {
              "$ref" : "#/components/schemas/UpdateRowRequest"
            }
          }
        },
        "description" : "The request body of every atp-action-controller operation. Each operation reads only projectName, envName, systemName, and title-table, plus the one list field that names it."
      },
      "TableAvailableDataStats" : {
        "type" : "object",
        "properties" : {
          "options" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "integer",
              "description" : "Number of available rows for each value found in the chosen column, keyed by that value.",
              "format" : "int32"
            },
            "description" : "Number of available rows for each value found in the chosen column, keyed by that value."
          },
          "tableName" : {
            "type" : "string",
            "description" : "Database table name of the table."
          },
          "tableTitle" : {
            "type" : "string",
            "description" : "Title of the table."
          }
        },
        "description" : "Number of available rows of one table, broken down by the value of a chosen column."
      },
      "TableColumnValues" : {
        "type" : "object",
        "properties" : {
          "tableName" : {
            "type" : "string",
            "description" : "Database table name of the table."
          },
          "tableTitle" : {
            "type" : "string",
            "description" : "Title of the table."
          },
          "values" : {
            "type" : "array",
            "description" : "Column values to count as available for this table's row in AvailableDataByColumnStats.",
            "items" : {
              "type" : "string",
              "description" : "Column values to count as available for this table's row in AvailableDataByColumnStats."
            }
          }
        },
        "description" : "Per-table filter: for each table, the column values to count as available. A table missing here counts every value."
      },
      "TdmDataToExport" : {
        "type" : "object",
        "properties" : {
          "projectId" : {
            "type" : "string",
            "description" : "Project ID.",
            "format" : "uuid"
          },
          "tableIdsList" : {
            "type" : "array",
            "description" : "Database table names of the tables to export.",
            "items" : {
              "type" : "string",
              "description" : "Database table names of the tables to export."
            }
          }
        },
        "description" : "Selection of test data tables to export, for the ATP Export Import service."
      },
      "TestAvailableDataMonitoring" : {
        "type" : "object",
        "properties" : {
          "activeColumn" : {
            "type" : "string",
            "description" : "Same value as description: name of the monitored column."
          },
          "description" : {
            "type" : "string",
            "description" : "Name of the monitored column, or an empty string when none is chosen yet."
          },
          "environmentId" : {
            "type" : "string",
            "description" : "Environment ID.",
            "format" : "uuid"
          },
          "recipients" : {
            "type" : "string",
            "description" : "Comma-separated email addresses to notify."
          },
          "schedule" : {
            "type" : "string",
            "description" : "Quartz cron expression, starting with the seconds field."
          },
          "scheduled" : {
            "type" : "boolean",
            "description" : "Runs the check on the schedule when set."
          },
          "systemId" : {
            "type" : "string",
            "description" : "System ID.",
            "format" : "uuid"
          },
          "threshold" : {
            "type" : "integer",
            "description" : "Sends the email when the available count drops below this threshold.",
            "format" : "int32"
          }
        },
        "description" : "Available-data monitoring schedule of one system's environment: a periodic email when a chosen column's available-row count drops below a threshold."
      },
      "TestDataCleanupConfig" : {
        "type" : "object",
        "properties" : {
          "enabled" : {
            "type" : "boolean",
            "description" : "Runs the cleanup on the schedule when set; a saved but disabled configuration can still be run with POST /api/tdm/cleanup/run."
          },
          "id" : {
            "type" : "string",
            "description" : "Cleanup configuration ID. Generated when the configuration is saved.",
            "format" : "uuid"
          },
          "queryTimeout" : {
            "type" : "integer",
            "description" : "Timeout of searchSql, in seconds, from 1 to EXTERNAL_QUERY_MAX_TIMEOUT (3600 by default).",
            "format" : "int32"
          },
          "schedule" : {
            "type" : "string",
            "description" : "Quartz cron expression, starting with the seconds field."
          },
          "scheduled" : {
            "type" : "string"
          },
          "searchClass" : {
            "type" : "string",
            "description" : "Name of the cleaner class to run. Read when type is CLASS; see the note on type."
          },
          "searchDate" : {
            "type" : "string",
            "description" : "Age of the rows to remove, as a number of weeks and days before now, such as \"3w4d\". Read when type is DATE; compared against the row's CREATED_WHEN."
          },
          "searchSql" : {
            "type" : "string",
            "description" : "SQL query that returns the rows to remove. Read when type is SQL."
          },
          "shared" : {
            "type" : "boolean",
            "description" : "Applies this configuration to the tables of every environment with the same title and system, instead of to one table."
          },
          "type" : {
            "type" : "string",
            "description" : "Which of searchSql, searchDate, or searchClass finds the rows to remove. searchClass is accepted but always rejected in this build: no cleaner class is registered.",
            "enum" : [
              "SQL",
              "DATE",
              "CLASS"
            ]
          }
        },
        "description" : "Which rows a cleanup removes from a table, and the cron schedule to run it on."
      },
      "TestDataFlagsTable" : {
        "type" : "object",
        "properties" : {
          "tableName" : {
            "type" : "string",
            "description" : "Database table name of the table."
          },
          "unoccupiedValidation" : {
            "type" : "boolean"
          }
        }
      },
      "TestDataRefreshConfig" : {
        "type" : "object",
        "properties" : {
          "allEnv" : {
            "type" : "boolean",
            "description" : "Also refreshes the tables of the project with the same title and the same import query."
          },
          "enabled" : {
            "type" : "boolean",
            "description" : "Runs the refresh on the schedule when set; a saved but disabled configuration can still be run with POST /api/tdm/data/refresh/run."
          },
          "id" : {
            "type" : "string",
            "description" : "Refresh configuration ID. Generated when the configuration is saved.",
            "format" : "uuid"
          },
          "queryTimout" : {
            "type" : "integer",
            "description" : "Not read or returned by the REST API; PUT /api/tdm/data/refresh/config sets the query timeout through its own queryTimeout parameter instead.",
            "format" : "int32"
          },
          "schedule" : {
            "type" : "string",
            "description" : "Quartz cron expression, starting with the seconds field."
          },
          "scheduled" : {
            "type" : "boolean"
          }
        },
        "description" : "Refresh schedule of a table."
      },
      "TestDataRequest" : {
        "type" : "object",
        "properties" : {
          "dataTableOrder" : {
            "$ref" : "#/components/schemas/TestDataTableOrder"
          },
          "filters" : {
            "type" : "array",
            "description" : "Column filters to apply.",
            "items" : {
              "$ref" : "#/components/schemas/TestDataTableFilter"
            }
          },
          "limit" : {
            "type" : "integer",
            "description" : "Maximum number of rows to return.",
            "format" : "int32"
          },
          "occupied" : {
            "type" : "boolean",
            "description" : "Reads occupied rows instead of available ones."
          },
          "offset" : {
            "type" : "integer",
            "description" : "Number of rows to skip, for paging.",
            "format" : "int32"
          },
          "tableName" : {
            "type" : "string",
            "description" : "Database table name of the table to read."
          }
        }
      },
      "TestDataTable" : {
        "type" : "object",
        "properties" : {
          "columns" : {
            "type" : "array",
            "description" : "Columns of the table, in storage order. Not written to the JSON output; see the class description.",
            "items" : {
              "$ref" : "#/components/schemas/TestDataTableColumn"
            }
          },
          "data" : {
            "type" : "array",
            "description" : "Rows of the table, one map of column name to value per row.",
            "items" : {
              "type" : "object",
              "additionalProperties" : {
                "type" : "object",
                "description" : "Rows of the table, one map of column name to value per row."
              },
              "description" : "Rows of the table, one map of column name to value per row."
            }
          },
          "name" : {
            "type" : "string",
            "description" : "Database table name of the table."
          },
          "query" : {
            "type" : "string",
            "description" : "Import query that fills the table."
          },
          "records" : {
            "type" : "integer",
            "description" : "Number of rows in data.",
            "format" : "int32"
          },
          "title" : {
            "type" : "string",
            "description" : "Title of the table. Not written to the JSON output; see the class description."
          },
          "type" : {
            "type" : "string",
            "description" : "Whether data holds available or occupied rows. Not written to the JSON output; see the class description.",
            "enum" : [
              "ALL",
              "AVAILABLE",
              "OCCUPIED"
            ]
          },
          "updateByQuery" : {
            "type" : "string",
            "description" : "Query that updates the table's rows from POST /api/tdm/update/sql."
          }
        },
        "description" : "Content of a test data table, for the TDM UI grid. TableSerializer writes this class to a nested {header: {rows}, body: {rows}, records, name, query, updateByQuery} shape rather than these fields directly; title, columns, and type do not appear in the JSON output."
      },
      "TestDataTableCatalog" : {
        "type" : "object",
        "properties" : {
          "cleanupConfigId" : {
            "type" : "string",
            "description" : "ID of the table's cleanup configuration, or null when none is set.",
            "format" : "uuid"
          },
          "environmentId" : {
            "type" : "string",
            "description" : "Environment ID.",
            "format" : "uuid"
          },
          "importQuery" : {
            "type" : "string",
            "description" : "Query that imports the table's data, from its refresh configuration; not persisted in this entity's own table."
          },
          "lastUsage" : {
            "type" : "string",
            "description" : "When the table was last read or written.",
            "format" : "date-time"
          },
          "projectId" : {
            "type" : "string",
            "description" : "Project ID.",
            "format" : "uuid"
          },
          "queryTimeout" : {
            "type" : "integer",
            "description" : "Timeout of importQuery, in seconds; not persisted in this entity's own table.",
            "format" : "int32"
          },
          "refreshConfigId" : {
            "type" : "string",
            "description" : "ID of the table's refresh configuration, or null when none is set.",
            "format" : "uuid"
          },
          "systemId" : {
            "type" : "string",
            "description" : "System ID.",
            "format" : "uuid"
          },
          "tableName" : {
            "type" : "string",
            "description" : "Database table name of the table."
          },
          "tableTitle" : {
            "type" : "string",
            "description" : "Title of the table."
          }
        }
      },
      "TestDataTableColumn" : {
        "type" : "object",
        "properties" : {
          "bulkLink" : {
            "type" : "boolean",
            "description" : "Splits a LINK column's value on \";\" into several links instead of one."
          },
          "columnLink" : {
            "type" : "string",
            "description" : "URL template for the column's values, read when columnType is LINK."
          },
          "columnType" : {
            "type" : "string",
            "description" : "How the column's values are typed and rendered.",
            "enum" : [
              "TEXT",
              "DATE",
              "LINK"
            ]
          },
          "filterType" : {
            "type" : "string",
            "description" : "Not persisted in this entity's own table.",
            "enum" : [
              "TEXT",
              "DATE",
              "LIST",
              "NONE"
            ]
          },
          "identity" : {
            "$ref" : "#/components/schemas/TestDataTableColumnIdentity"
          },
          "orderType" : {
            "type" : "string",
            "description" : "Not persisted in this entity's own table.",
            "enum" : [
              "ASC",
              "DESC",
              "TRUE"
            ]
          }
        },
        "description" : "Columns of the table, in storage order. Not written to the JSON output; see the class description."
      },
      "TestDataTableColumnIdentity" : {
        "type" : "object",
        "properties" : {
          "columnName" : {
            "type" : "string",
            "description" : "Column name."
          },
          "tableName" : {
            "type" : "string",
            "description" : "Database table name of the table."
          }
        },
        "description" : "Table and column name."
      },
      "TestDataTableFilter" : {
        "type" : "object",
        "properties" : {
          "caseSensitive" : {
            "type" : "boolean",
            "description" : "Matches the column case-sensitively. Ignored by \"From\" and \"To\"."
          },
          "name-column" : {
            "type" : "string",
            "description" : "Name of the column to search in."
          },
          "search-criterion" : {
            "type" : "string",
            "description" : "\"Contains\", \"startWith\", \"Equals\", \"From\", or \"To\", case-insensitive."
          },
          "search-values" : {
            "type" : "array",
            "description" : "Values to compare the column against. Only the first value is read; the others are ignored.",
            "items" : {
              "type" : "string",
              "description" : "Values to compare the column against. Only the first value is read; the others are ignored."
            }
          }
        },
        "description" : "One search condition on a column, used to filter the rows of a table."
      },
      "TestDataTableMonitoring" : {
        "type" : "object",
        "properties" : {
          "cronExpression" : {
            "type" : "string",
            "description" : "Quartz cron expression, starting with the seconds field."
          },
          "enabled" : {
            "type" : "boolean",
            "description" : "Runs the check on the schedule when set."
          },
          "id" : {
            "type" : "string",
            "format" : "uuid"
          },
          "projectId" : {
            "type" : "string",
            "description" : "Project ID.",
            "format" : "uuid"
          },
          "recipients" : {
            "type" : "string",
            "description" : "Comma-separated email addresses to notify."
          },
          "schedule" : {
            "type" : "string"
          },
          "scheduled" : {
            "type" : "string"
          },
          "threshold" : {
            "type" : "integer",
            "description" : "Sends the email for a table when its available count drops below this threshold.",
            "format" : "int32"
          }
        },
        "description" : "General-statistics monitoring schedule of one project: a periodic email when a table's available-row count drops below a threshold."
      },
      "TestDataTableOrder" : {
        "type" : "object",
        "properties" : {
          "columnName" : {
            "type" : "string",
            "description" : "Name of the column to sort by."
          },
          "orderType" : {
            "type" : "string",
            "description" : "Sort direction: ASC or DESC.",
            "enum" : [
              "ASC",
              "DESC",
              "TRUE"
            ]
          }
        },
        "description" : "Column and direction to sort by."
      },
      "TestDataTableUpdateByQuery" : {
        "type" : "object",
        "properties" : {
          "environmentId" : {
            "type" : "string",
            "description" : "Environment ID.",
            "format" : "uuid"
          },
          "projectId" : {
            "type" : "string",
            "description" : "Project ID.",
            "format" : "uuid"
          },
          "query" : {
            "type" : "string",
            "description" : "SQL query that returns the new rows."
          },
          "queryTimeout" : {
            "type" : "integer",
            "description" : "Query timeout, in seconds.",
            "format" : "int32"
          },
          "systemId" : {
            "type" : "string",
            "description" : "System ID whose DB connection runs the query.",
            "format" : "uuid"
          },
          "tableName" : {
            "type" : "string",
            "description" : "Database table name of the test data table."
          }
        },
        "description" : "Request body of POST /api/tdm/update/sql: runs a query against a system and updates a table's rows from its result. The query and timeout are saved with the table."
      },
      "TestDataTableUsersMonitoring" : {
        "type" : "object",
        "properties" : {
          "cronExpression" : {
            "type" : "string",
            "description" : "Quartz cron expression, starting with the seconds field."
          },
          "csvReport" : {
            "type" : "boolean",
            "description" : "Attaches a CSV file of the occupations to the email."
          },
          "daysCount" : {
            "type" : "integer",
            "description" : "Number of past days the report covers.",
            "format" : "int32"
          },
          "enabled" : {
            "type" : "boolean",
            "description" : "Sends the report on the schedule when set."
          },
          "htmlReport" : {
            "type" : "boolean",
            "description" : "Includes an HTML table of the occupations in the email body."
          },
          "id" : {
            "type" : "string",
            "format" : "uuid"
          },
          "projectId" : {
            "type" : "string",
            "description" : "Project ID.",
            "format" : "uuid"
          },
          "recipients" : {
            "type" : "string",
            "description" : "Comma-separated email addresses to notify."
          },
          "schedule" : {
            "type" : "string"
          },
          "scheduled" : {
            "type" : "boolean"
          }
        },
        "description" : "Users-occupation-report schedule of one project: a periodic email of who occupied which rows."
      },
      "UpdateRowRequest" : {
        "type" : "object",
        "properties" : {
          "record-with-data-for-update" : {
            "type" : "object",
            "additionalProperties" : {
              "type" : "string",
              "description" : "Column names and the values to write to them, in every row that matches the filters."
            },
            "description" : "Column names and the values to write to them, in every row that matches the filters."
          },
          "search-row-parameters-set" : {
            "type" : "array",
            "description" : "Row to act on: every condition must match. The row is found among the available rows, except where the request's own operation reads the occupied rows instead.",
            "items" : {
              "$ref" : "#/components/schemas/ApiDataFilter"
            }
          }
        },
        "description" : "Values to write to the matching rows. Each value in record-with-data-for-update replaces the current value of its column."
      },
      "UsersOccupyStatisticRequest" : {
        "type" : "object",
        "properties" : {
          "dataTableOrder" : {
            "$ref" : "#/components/schemas/TestDataTableOrder"
          },
          "dateFrom" : {
            "type" : "string",
            "description" : "First day of the period, as yyyy-MM-dd."
          },
          "dateTo" : {
            "type" : "string",
            "description" : "Last day of the period, as yyyy-MM-dd."
          },
          "filters" : {
            "type" : "array",
            "description" : "Filters on the table title, the user name, the system name, or the environment name. \"From\" and \"To\" are not applied here.",
            "items" : {
              "$ref" : "#/components/schemas/TestDataTableFilter"
            }
          },
          "limit" : {
            "type" : "integer",
            "description" : "Maximum number of rows to return.",
            "format" : "int64"
          },
          "offset" : {
            "type" : "integer",
            "description" : "Number of rows to skip, for paging.",
            "format" : "int64"
          },
          "projectId" : {
            "type" : "string",
            "description" : "Project ID.",
            "format" : "uuid"
          }
        }
      },
      "UsersOccupyStatisticResponse" : {
        "type" : "object",
        "properties" : {
          "data" : {
            "type" : "array",
            "description" : "The page of records, up to limit entries.",
            "items" : {
              "$ref" : "#/components/schemas/OccupiedDataByUsersStatistics"
            }
          },
          "records" : {
            "type" : "integer",
            "description" : "Total number of records matching the request, across every page.",
            "format" : "int32"
          }
        },
        "description" : "One page of occupation records by user."
      },
      "VersionDto" : {
        "type" : "object",
        "properties" : {
          "name" : {
            "type" : "string",
            "description" : "Component name."
          },
          "version" : {
            "type" : "string",
            "description" : "Component version."
          }
        },
        "description" : "Name and version of the service, or of one of its dependencies."
      }
    },
    "securitySchemes" : {
      "Bearer Authentication" : {
        "bearerFormat" : "JWT",
        "scheme" : "bearer",
        "type" : "http"
      }
    }
  }
}
